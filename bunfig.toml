[test]
# Installs the offline fetch guard before any test file runs, so a request that forgets to stub
# fetch fails instead of reaching docs.google.com or the Fellow API.
preload = ["./test/helpers/offline.ts"]
